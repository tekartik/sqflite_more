name: sqflite_more_repo_support
description: App flutter utils repo (tools)
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  process_run: '>=1.2.0'
  dev_build: '>=1.0.0'
  path:
  pub_semver:
  test:


